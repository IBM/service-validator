---
language: python
python: "3.7"

notifications:
  email: true

jobs:
  include:

    - stage: linter and code format
      script:
        - pip install pre-commit
        - pre-commit run --all-files
